import { ColorScheme, MenuMode, MenuProfileMode } from '../layout/service/layout.service';

export interface ThemeSettings {
  colorScheme: ColorScheme;
  menuMode: MenuMode;
  menuProfileMode: MenuProfileMode;
}
